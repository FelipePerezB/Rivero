// import { RemoveGradeDocument } from "src/gql/graphql";
import { client } from "src/service/client";

const removeGrade = async (id: number) =>""
  // await client.mutate({
  //   mutation: RemoveGradeDocument,
  //   variables: {
  //     removeGradeId: id,
  //   },
  // });

export default removeGrade;
