// import { RemoveDocDocument } from "src/gql/graphql";
import { client } from "src/service/client";

const removeDoc = async (id: number) =>""
  // await client.mutate({
  //   mutation: RemoveDocDocument,
  //   variables: {
  //     removeDocId: id,
  //   },
  // });

export default removeDoc;
